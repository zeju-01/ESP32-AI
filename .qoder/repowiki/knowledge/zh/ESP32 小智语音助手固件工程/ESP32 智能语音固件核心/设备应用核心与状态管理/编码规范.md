- 核心管理类（如Application, McpServer, ReminderManager）普遍采用单例模式（Singleton）确保全局唯一访问。
- 关键业务类（如Application, DeviceStateMachine）显式删除拷贝构造函数和赋值运算符，防止非法复制。
- 配置与状态数据统一通过NVS（Non-Volatile Storage）进行持久化，由Settings类或各模块内部直接操作。