- MCP协议消息必须封装在基础通信协议（WebSocket/MQTT）的payload中，并严格遵循JSON-RPC 2.0规范（包含jsonrpc, method, params, id字段）。
- 设备与后台的握手消息（hello）必须包含transport字段以标识底层传输方式（websocket或udp），并携带audio_params进行音频参数协商。
- 物联网控制统一采用MCP协议的tools/list和tools/call方法，废弃原有的iot类型消息，工具命名建议采用“模块.功能”的层次化风格。